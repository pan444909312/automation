package com.miller.userapp.module.shop.card.version2.utils;

public class AutoSignUtils {
    // 示例方法
    public static String generateAutoSign(String data) {
        // 实现签名逻辑，比如 SHA256 + HEX
        return "signed_" + data;
    }
}