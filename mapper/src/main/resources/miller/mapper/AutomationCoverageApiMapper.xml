<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miller.mapper.report.AutomationCoverageApiMapper">

    <select id="findByTestCaseRequestPath" resultType="com.miller.entity.report.AutomationCoverageApiEntity">
        SELECT *
        FROM automation_coverage_api
        WHERE path = #{path} and api_status=0 and is_automation=0
    </select>


</mapper>
