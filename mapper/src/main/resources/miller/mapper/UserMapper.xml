<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miller.mapper.platform.UserMapper">

    <select id="getProjectListByUserIdOrEmail" resultType="java.lang.String">
        SELECT ubp.project_id
        FROM user u
                 LEFT OUTER JOIN
             user_bind_project ubp ON u.user_id = ubp.user_id
        WHERE u.user_id = #{userId}
           or u.email = #{userId};
    </select>
</mapper>
